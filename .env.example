# URL da API backend (usada pelo frontend).
# - Desenvolvimento local: aponte para o backend -> http://localhost:3000
# - Produção no Render (frontend + backend no mesmo serviço): DEIXE VAZIO ou
#   não defina. O frontend passa a usar caminho relativo (mesma origem).
VITE_API_URL="http://localhost:3000"
