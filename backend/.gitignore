node_modules
# Keep environment variables out of version control
.env

/generated/prisma

# Banco SQLite local (no Render fica em /tmp ou /data via Persistent Disk)
prisma/*.db
prisma/*.db-journal
prisma/*.db-shm
prisma/*.db-wal
# Pasta gerada por engano pela resolução relativa do Prisma
prisma/prisma/
